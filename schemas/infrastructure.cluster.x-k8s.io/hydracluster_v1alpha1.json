{
  "description": "HydraCluster is the Schema for the hydraclusters API.\n\nIt is referenced by a Cluster's spec.infrastructureRef, which is what makes\nthe Cluster admissible at all: Cluster API rejects a Cluster carrying none of\ninfrastructureRef, controlPlaneRef or topology.",
  "properties": {
    "apiVersion": {
      "description": "APIVersion defines the versioned schema of this representation of an object.\nServers should convert recognized schemas to the latest internal value, and\nmay reject unrecognized values.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources",
      "type": "string"
    },
    "kind": {
      "description": "Kind is a string value representing the REST resource this object represents.\nServers may infer this from the endpoint the client submits requests to.\nCannot be updated.\nIn CamelCase.\nMore info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds",
      "type": "string"
    },
    "metadata": {
      "type": "object"
    },
    "spec": {
      "description": "spec defines the desired state of HydraCluster",
      "properties": {
        "baseImage": {
          "description": "baseImage is the image machines in this cluster boot from when they do not\nname one of their own.\n\nEmpty falls back to the manager's --libvirt-base-image flag.",
          "properties": {
            "checksum": {
              "description": "checksum verifies a fetched image, in the form \"<algorithm>:<hex>\".\n\nThe digest length is pinned to the algorithm -- 64 hex characters for\nsha256, 128 for sha512 -- so a truncated or malformed digest is rejected at\nadmission rather than discovered halfway through provisioning a machine.\n\nOnly valid alongside url; a checksum with nothing to verify is rejected.",
              "pattern": "^(sha256:[a-fA-F0-9]{64}|sha512:[a-fA-F0-9]{128})$",
              "type": "string"
            },
            "name": {
              "description": "name identifies a base image already known to the backend.",
              "maxLength": 253,
              "minLength": 1,
              "type": "string"
            },
            "url": {
              "description": "url is where the backend may fetch the image from when it does not already\nhold one matching name.",
              "maxLength": 2048,
              "minLength": 1,
              "type": "string"
            }
          },
          "type": "object",
          "x-kubernetes-validations": [
            {
              "message": "one of name or url must be set",
              "rule": "has(self.name) || has(self.url)"
            },
            {
              "message": "checksum is only meaningful alongside url",
              "rule": "!has(self.checksum) || has(self.url)"
            }
          ],
          "additionalProperties": false
        },
        "controlPlaneEndpoint": {
          "description": "controlPlaneEndpoint is the address and port of this cluster's API server.\n\nCluster API reads it to populate the Cluster's own endpoint. Providers that\ncreate the endpoint themselves -- with a load balancer or a VIP -- are\nexpected to fill it in. Hydra creates neither, so this is supplied by\nwhoever writes the object, and the provider only reports it back.\n\nImmutable once set. Moving a live cluster's API endpoint is not something\na reconcile can carry out; it is a new cluster.\n\nRequired in practice, and enforced by a rule on the spec rather than by\noptional and the struct itself omitempty, so +required would still admit\n{host: \"x\"} with port 0 -- an endpoint that satisfies the schema and cannot\nbe dialled. Reporting infrastructure provisioned with no usable endpoint\nleaves Cluster API with nothing to copy and kubeadm with nowhere to join.\n\nNOTE: this field is part of the Cluster API contract.",
          "minProperties": 1,
          "properties": {
            "host": {
              "description": "host is the hostname on which the API server is serving.",
              "maxLength": 512,
              "minLength": 1,
              "type": "string"
            },
            "port": {
              "description": "port is the port on which the API server is serving.",
              "format": "int32",
              "maximum": 65535,
              "minimum": 1,
              "type": "integer"
            }
          },
          "type": "object",
          "x-kubernetes-validations": [
            {
              "message": "controlPlaneEndpoint.host is immutable once set",
              "rule": "!has(oldSelf.host) || (has(self.host) && self.host == oldSelf.host)"
            },
            {
              "message": "controlPlaneEndpoint.port is immutable once set",
              "rule": "!has(oldSelf.port) || (has(self.port) && self.port == oldSelf.port)"
            }
          ],
          "additionalProperties": false
        },
        "managedNetwork": {
          "description": "managedNetwork asks Hydra to create and own a network for this cluster,\ninstead of only attaching machines to networks an operator prepared.\n\nIt exists to solve the control-plane endpoint problem. A\nKubeadmControlPlane needs an endpoint that survives replicas coming and\ngoing, so controlPlaneEndpoint must be an address no DHCP server will ever\nhand to a machine -- and it is immutable, so a wrong guess is not\nrepairable. On a bridged site network there is no way to know that: the\naddresses a DHCP server has already issued say nothing about the bounds of\nits pool, because many allocate by hashing the client ID rather than\ncounting upward.\n\nDeclaring one here makes the answer structural. Hydra sets the DHCP range,\nso which addresses can be handed to a machine is known rather than guessed\nat from someone else's router.\n\n\"Outside the range\" is not the same as \"usable\", and the difference has\nbitten before. The network address, the broadcast address and the gateway\n-- libvirt takes the first host address -- all sit outside the range and\nnone can serve an endpoint. The controller refuses a controlPlaneEndpoint\non any of them, and on anything inside the range, because the field is\nimmutable and the alternative is a cluster that admits cleanly and then\ncannot be reached.\n\nMachines receive an interface on this network IN ADDITION to networks\nabove, not instead of them -- the site network is what gives them internet\naccess and the address they register as Nodes.",
          "properties": {
            "dhcpEnd": {
              "pattern": "^(([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5])\\.){3}([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5])$",
              "type": "string"
            },
            "dhcpStart": {
              "description": "dhcpStart and dhcpEnd bound the addresses handed to machines.\n\nThe point of stating them is what they LEAVE OUT: a controlPlaneEndpoint\noutside this range cannot be handed to a machine, which is the guarantee a\nbridged site network cannot give.\n\nOutside the range still leaves three addresses that were never available\n-- the network address, the broadcast address, and the gateway on the first\nhost address -- so the usable space is the subnet minus those minus this\nrange. The controller checks it rather than trusting the arithmetic.\n\nLeave room. A range covering the whole subnet is accepted, because\nrefusing it would be second-guessing an operator who has some other plan\nfor the endpoint, but it gives Hydra nothing to allocate from.",
              "pattern": "^(([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5])\\.){3}([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5])$",
              "type": "string"
            },
            "name": {
              "description": "name is the network's name on the hypervisor.\n\nHydra creates it if it is absent and verifies it if it is not, so naming an\nexisting network adopts it -- which is why the checks are strict about the\nsubnet and range matching rather than reconfiguring what it finds.",
              "maxLength": 63,
              "minLength": 1,
              "pattern": "^[a-z0-9]([-a-z0-9]*[a-z0-9])?$",
              "type": "string"
            },
            "subnet": {
              "description": "subnet is the address range of the network, in CIDR form.\n\nHydra takes the first address for the gateway, the way libvirt's own\nnetworks do.",
              "pattern": "^(([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5])\\.){3}([0-9]|[1-9][0-9]|1[0-9]{2}|2[0-4][0-9]|25[0-5])/([0-9]|[12][0-9]|3[0-2])$",
              "type": "string"
            }
          },
          "required": [
            "dhcpEnd",
            "dhcpStart",
            "name",
            "subnet"
          ],
          "type": "object",
          "x-kubernetes-validations": [
            {
              "message": "managedNetwork is immutable; machines are attached to it for their whole lives",
              "rule": "self == oldSelf"
            }
          ],
          "additionalProperties": false
        },
        "networks": {
          "description": "networks are the network attachments machines in this cluster receive when\nthey do not name any of their own.\n\nThere is no fallback for this one. A machine with no network attachment\nboots and can never reach an API server, so provisioning is refused rather\nthan producing something that looks healthy to the backend and is useless.\n\nThe map-list markers mirror HydraMachine's, so a duplicate attachment name\nis rejected here too. Without them an inherited duplicate would produce two\nNICs on one attachment, while the identical list written directly on a\nmachine would be refused -- the same list admitted or rejected depending\nonly on which object it was written on.\n\nMinItems is deliberately NOT mirrored. This list is a default, and empty is\na meaningful value: \"machines in this cluster name their own attachments\".\nRequiring one would make that arrangement inexpressible.",
          "items": {
            "description": "HydraNetworkAttachment attaches a machine to one backend network.",
            "properties": {
              "name": {
                "description": "name is the backend network or bridge to attach to, for example \"br0\".\nHydra does not interpret this value; it is passed to the backend, which\ndecides what it means.",
                "maxLength": 253,
                "minLength": 1,
                "type": "string"
              }
            },
            "required": [
              "name"
            ],
            "type": "object",
            "additionalProperties": false
          },
          "maxItems": 8,
          "type": "array",
          "x-kubernetes-list-map-keys": [
            "name"
          ],
          "x-kubernetes-list-type": "map"
        },
        "storagePool": {
          "description": "storagePool is the backend storage pool this cluster's machine disks are\ncreated in, and where its base image is expected to live.\n\nCluster-scoped on purpose, with no per-machine override. A pool per machine\nis the arrangement found on the reference hardware -- one pool each for\nthree hand-built VMs -- and it is precisely the shape this provider cannot\nuse, because it resolves the base image as a volume *inside* the configured\npool. One pool per cluster keeps a single place the image can be.\n\nEmpty falls back to the manager's --libvirt-storage-pool flag. When both\nare empty the failure is reported terminally against this object, which is\nthe one that can be fixed -- the backend deliberately no longer refuses to\nstart over an unset pool, because that made the flag mandatory process-wide\nand so made this field pointless.\n\nFixed when the cluster is created, and that includes whether it is set at\nall. Set-once is not enough: omitting it already selects the manager's\npool, so allowing omitted-to-set would put existing machines in one pool\nand every machine created afterwards in another -- the split-pool state\nthis field's whole purpose is to avoid. A reconcile cannot carry out that\nchange either: existing machines were cloned from a backing volume inside\nthe old pool and keep referring to it, and provisioned does not regress,\nso swapping in a missing pool would not even be reported.",
          "maxLength": 253,
          "minLength": 1,
          "type": "string"
        }
      },
      "type": "object",
      "x-kubernetes-validations": [
        {
          "message": "controlPlaneEndpoint requires both a host and a non-zero port",
          "rule": "has(self.controlPlaneEndpoint) && size(self.controlPlaneEndpoint.host) > 0 && self.controlPlaneEndpoint.port > 0"
        },
        {
          "message": "storagePool is fixed when the cluster is created; omitting it selects the manager's pool, which is equally binding",
          "rule": "has(self.storagePool) == has(oldSelf.storagePool) && (!has(self.storagePool) || self.storagePool == oldSelf.storagePool)"
        },
        {
          "message": "managedNetwork cannot be added or removed after the cluster is created; machines keep the attachments they were built with, so either change leaves the control-plane endpoint on a network only part of the cluster is attached to",
          "rule": "has(self.managedNetwork) == has(oldSelf.managedNetwork)"
        }
      ],
      "additionalProperties": false
    },
    "status": {
      "description": "status defines the observed state of HydraCluster",
      "properties": {
        "conditions": {
          "description": "conditions represent the current state of the HydraCluster resource.\n\nA condition of type \"Ready\" is mirrored into the Cluster's\nInfrastructureReady condition.",
          "items": {
            "description": "Condition contains details for one aspect of the current state of this API Resource.",
            "properties": {
              "lastTransitionTime": {
                "description": "lastTransitionTime is the last time the condition transitioned from one status to another.\nThis should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.",
                "format": "date-time",
                "type": "string"
              },
              "message": {
                "description": "message is a human readable message indicating details about the transition.\nThis may be an empty string.",
                "maxLength": 32768,
                "type": "string"
              },
              "observedGeneration": {
                "description": "observedGeneration represents the .metadata.generation that the condition was set based upon.\nFor instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date\nwith respect to the current state of the instance.",
                "format": "int64",
                "minimum": 0,
                "type": "integer"
              },
              "reason": {
                "description": "reason contains a programmatic identifier indicating the reason for the condition's last transition.\nProducers of specific condition types may define expected values and meanings for this field,\nand whether the values are considered a guaranteed API.\nThe value should be a CamelCase string.\nThis field may not be empty.",
                "maxLength": 1024,
                "minLength": 1,
                "pattern": "^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$",
                "type": "string"
              },
              "status": {
                "description": "status of the condition, one of True, False, Unknown.",
                "enum": [
                  "True",
                  "False",
                  "Unknown"
                ],
                "type": "string"
              },
              "type": {
                "description": "type of condition in CamelCase or in foo.example.com/CamelCase.",
                "maxLength": 316,
                "pattern": "^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$",
                "type": "string"
              }
            },
            "required": [
              "lastTransitionTime",
              "message",
              "reason",
              "status",
              "type"
            ],
            "type": "object",
            "additionalProperties": false
          },
          "type": "array",
          "x-kubernetes-list-map-keys": [
            "type"
          ],
          "x-kubernetes-list-type": "map"
        },
        "initialization": {
          "description": "initialization reports whether the cluster's infrastructure is ready.\n\nNOTE: this field is part of the Cluster API contract.",
          "properties": {
            "provisioned": {
              "description": "provisioned reports that the cluster-scoped infrastructure this provider\ndepends on has been verified.\n\nCluster API reads this to populate the Cluster's\nstatus.initialization.infrastructureProvisioned, which gates machine\ncreation. Until it is true, no HydraMachine in this cluster is created.\n\nNOTE: this field is part of the Cluster API contract.",
              "type": "boolean"
            }
          },
          "type": "object",
          "additionalProperties": false
        }
      },
      "type": "object",
      "additionalProperties": false
    }
  },
  "required": [
    "spec"
  ],
  "type": "object"
}
